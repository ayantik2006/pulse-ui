import { useState } from "react";

function DatePicker({ selectedDate }: { selectedDate: (date: Date) => void }) {
  const [date,setDate]=useState<Date>();
  
  return (
    <div className="">
        
    </div>
  )
}

export default DatePicker;
